'use strict'

function split_Words(str){
    let result=str.split(" ")
    return result;
}

let str="Everything happens for a reason";

let result= split_Words(str);

console.log(str);
console.log(result);